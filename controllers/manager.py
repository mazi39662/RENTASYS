def manager():
    # Redirect to the desired URL when the "Manager" link is clicked.
    return dict()
    # redirect(URL('default', 'tenant'))

def bu_manager():
    # Redirect to the desired URL when the "Manager" link is clicked.
    return dict()